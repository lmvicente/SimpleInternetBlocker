<Window x:Class="SimpleInternetBlock.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Firewall Application Blocker" 
        Height="500" 
        Width="700"
        Background="#F5F5F5"
        ResizeMode="CanMinimize">

    <Window.Resources>
        <!-- Modern Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#005A9E"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#004578"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Secondary Button Style -->
        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#6C757D"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#5A6268"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Danger Button Style -->
        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#DC3545"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#C82333"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="Block Applications from Internet Access" 
                   FontSize="20" 
                   FontWeight="Bold"
                   Foreground="#333333"
                   Margin="0,0,0,20"/>

        <!-- Path Input Section -->
        <Border Grid.Row="1" 
                Background="White" 
                CornerRadius="8" 
                Padding="15"
                Margin="0,0,0,15">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" 
                                  ShadowDepth="2" 
                                  Opacity="0.1"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Path TextBox -->
                <TextBox x:Name="txtPath" 
                         Grid.Row="0" 
                         Grid.Column="0"
                         Height="35"
                         VerticalContentAlignment="Center"
                         Padding="10,0"
                         FontSize="14"
                         Margin="0,0,10,10"
                         BorderBrush="#CCCCCC"
                         BorderThickness="1">
                    <TextBox.Resources>
                        <Style TargetType="{x:Type Border}">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </TextBox.Resources>
                </TextBox>

                <!-- Browse Button -->
                <Button x:Name="btnBrowse" 
                        Grid.Row="0" 
                        Grid.Column="1"
                        Content="Browse..." 
                        Style="{StaticResource SecondaryButton}"
                        Width="120"
                        Height="35"
                        Margin="0,0,0,10"
                        Click="BtnBrowse_Click"/>

                <!-- Checkbox and Block Button -->
                <CheckBox x:Name="chkIsFolder" 
                          Grid.Row="1" 
                          Grid.Column="0"
                          Content="Block entire folder (all .exe files)"
                          VerticalAlignment="Center"
                          FontSize="13"
                          Foreground="#555555"/>

                <Button x:Name="btnBlock" 
                        Grid.Row="1" 
                        Grid.Column="1"
                        Content="🛡️ Block" 
                        Style="{StaticResource ModernButton}"
                        Width="120"
                        Height="35"
                        Click="BtnBlock_Click"/>
            </Grid>
        </Border>

        <!-- Blocked Items Label -->
        <TextBlock Grid.Row="2" 
                   Text="Blocked Items" 
                   FontSize="16" 
                   FontWeight="SemiBold"
                   Foreground="#333333"
                   Margin="0,0,0,10"/>

        <!-- ListBox Section -->
        <Border Grid.Row="3" 
                Background="White" 
                CornerRadius="8"
                Margin="0,0,0,15"
                BorderBrush="#DDDDDD"
                BorderThickness="1">
            <ListBox x:Name="lstBlocked" 
                     BorderThickness="0"
                     Padding="5"
                     FontSize="13"
                     ScrollViewer.HorizontalScrollBarVisibility="Auto">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="10,8"/>
                        <Setter Property="Margin" Value="0,2"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#E3F2FD"/>
                                <Setter Property="Foreground" Value="#0078D4"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
        </Border>

        <!-- Unblock Button -->
        <Button x:Name="btnUnblock" 
                Grid.Row="4"
                Content="❌ Unblock Selected" 
                Style="{StaticResource DangerButton}"
                Width="191"
                Height="40"
                HorizontalAlignment="Right"
                Margin="0,0,0,15"
                Click="BtnUnblock_Click"/>

        <!-- Status Bar -->
        <Border Grid.Row="5" 
                Background="#0078D4" 
                CornerRadius="4"
                Padding="15,10">
            <TextBlock x:Name="txtStatus" 
                       Text="Ready" 
                       Foreground="White"
                       FontSize="13"/>
        </Border>
    </Grid>
</Window>